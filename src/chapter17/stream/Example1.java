package chapter17.stream;

import chapter15.json.Transaction;

import java.security.SecureRandom;
import java.util.Set;
import java.util.stream.Stream;

public class Example1 {
    public static void main(String[] args) {
//        Set<Integer> numbers = Set.of(20,30,40,50);
//        Stream<Integer> stream = numbers.stream();
//        stream.forEach(System.out::println);
//        Stream<String> stream1 = Stream.empty();
//        Stream<Transaction> transactionStream = Stream.of(new Transaction("45678", 100),
//                new Transaction("6789",600));
//        transactionStream.forEach((transaction) -> System.out.printf("account with id %s has %s in it", transaction.getAccountNumber(),transaction.getAmount()));

        //z6Stream<Integer> integerStream = Stream.generate(()->new SecureRandom().l)
    }
}
